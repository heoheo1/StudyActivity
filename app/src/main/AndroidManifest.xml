<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hj.studyactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_write_foreground"
        android:label="공존어플"
        android:roundIcon="@mipmap/ic_launcher_write_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyActivity">
        <activity
            android:name=".SubjectActivity"
            android:exported="true" />
        <activity
            android:name=".PictureActivtiy"
            android:exported="true" />
        <activity android:name=".WriteActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SingUpActivity" />
        <activity android:name=".MainActivity" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE " />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>